const { headerHeight, headerBackground, colors } = require("../variables");
const header = {
	selector: ".Header",
	color: colors.main,
	width: "100%",
	height: headerHeight,
	align: "center column",
	background: headerBackground
};

module.exports = {
	header
};
